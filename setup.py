from setuptools import setup, find_packages

setup(
    name="self_enhancer",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Fügen Sie hier Ihre Projektabhängigkeiten hinzu
    ],
)
